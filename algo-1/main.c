#include <stdio.h>

#include "algo.h"

/*** Basic algorithms, using comparators and loops ***/

int main(){
    compareAndContrast();
    sumNumHelper();
    mulTable(15);
    pascalTriangle(12);
    return 0;
}